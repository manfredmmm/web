/* Pixel to rem function */
set-font-size(value) {
  font-size: value;
  if (value is inherit) {
    font-size: inherit;
  } else {
    font-size: rem(value);
  }
}
rem(value) {
  u = unit(value);
  if (u is 'px') {
    return unit(value/16, 'rem');
  } else {
    return unit(value, u);
  }
}

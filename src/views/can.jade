section.can(ng-show="isCurrentPage('can')")
  .skills-table(ng-controller="canIUseController")
    .header.u-centered
      label {{ pages[currentPage].title }}
      input(ng-model="searchSkill" placeholder="Skill; for example, ask me for html" align="middle")
      span ?
    .content
      p search by: {{ searchSkill }}
      .family(ng-repeat="family in families | filter:searchSkill")
        h4.u-uppercase {{ family.name }}
        ul
          li(ng-repeat="skill in family.skills")
            a(ng-click="showLevel(skill)") {{ skill.name }} {{ skill.level }}
